// Instructions

/* Is every value in the array an array?

This should only test the second array dimension of the array. The values of the nested arrays don't have to be arrays.*/

const arrCheck = value => value.every(Array.isArray) ? true : false

// I did not pass this challenge. I knew how to solve it but I did not stop to do pseudo code and focus on the directions to realize what it was asking
